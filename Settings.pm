#!/usr/bin/perl
# Package: Settings
#   stores settings for whole project

package Settings;

use strict;
use Exporter;

use vars qw($LINK_PREFIX $VERSION);
$LINK_PREFIX = 'http://pl.wikinews.org';

# Const: $VERSION
#   program version
$VERSION = '0.4.4';


1;
